# Binaries
/bin/
/credentials-broker
*.exe

# Local instance config - contains real target/caller mappings, never commit
config.yaml
.env
.env.*
!.env.example
!config.example.yaml

# Local/docker-run SQLite database - contains real target/caller data, never commit
*.db
/data/

# Worktrees
.worktrees/

# IDE/OS
.DS_Store

# tokensave local index
.tokensave/

# superpowers subagent-driven-development scratch workspace
.superpowers/
